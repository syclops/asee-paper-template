% Any miscellaneous preamble commands can be done here.

% ASEE requires one line to be skipped between paragraphs.
\setlength{\parskip}{\baselineskip}

% This isn't strictly necessary for the ASEE style, but does look better with
% blank lines between paragraphs.
\setlength{\parindent}{0pt}

% Get rid of page numbers as required by ASEE.
\pagestyle{empty}
